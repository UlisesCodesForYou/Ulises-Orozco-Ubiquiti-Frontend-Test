import { createStyles } from '@mantine/core';

const useStyles = createStyles(() => ({
  productLabel: {
    color: '#000000',
    fontWeight: 700,
    fontSize: 20,
  },

  checkboxItems: {
    marginTop: '.5rem',
  },
}));

export default useStyles;
